# Fake server using json-server

Install packages:
```
yarn install
or 
npm install
```

Generate data:
```
npm run generate
or
yarn run generate
```

Run server:
```
npm run start
or
yarn run start
```
